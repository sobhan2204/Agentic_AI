[
    {
      "name": "send_email",
      "description": "Send an email from your Gmail account",
      "inputSchema": {
        "type": "object",
        "properties": {
          "to": {"type": "string", "description": "Recipient email address"},
          "subject": {"type": "string", "description": "Email subject"},
          "body": {"type": "string", "description": "Email body"}
        },
        "required": ["to", "subject", "body"]
      }
    },
    {
      "name": "schedule_email",
      "description": "Schedule an email to be sent at a specific time",
      "inputSchema": {
        "type": "object",
        "properties": {
          "to": {"type": "string", "description": "Recipient email address"},
          "subject": {"type": "string", "description": "Email subject"},
          "body": {"type": "string", "description": "Email body"},
          "send_time": {"type": "string", "description": "ISO 8601 datetime (e.g., 2025-07-15T10:00:00Z)"}
        },
        "required": ["to", "subject", "body", "send_time"]
      }
    },
    {
      "name": "search_emails",
      "description": "Search emails in your Gmail account",
      "inputSchema": {
        "type": "object",
        "properties": {
          "query": {"type": "string", "description": "Gmail search query (e.g., 'from:example@gmail.com')"},
          "limit": {"type": "number", "description": "Max results (default: 5)", "minimum": 1, "maximum": 10}
        },
        "required": ["query"]
      }
    },
    {
      "name": "read_email",
      "description": "Read an email by its ID",
      "inputSchema": {
        "type": "object",
        "properties": {
          "message_id": {"type": "string", "description": "Email message ID"}
        },
        "required": ["message_id"]
      }
    }
  ] 